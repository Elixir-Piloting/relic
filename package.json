{
  "name": "relic-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:desktop": "npm run dev --workspace=apps/desktop",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:web": "npm run build:shared && npm run build --workspace=apps/web",
    "build:desktop": "npm run build:shared && npm run build --workspace=apps/desktop",
    "build": "npm run build:shared && npm run build:web && npm run build:desktop",
    "lint": "npm run lint --workspaces --if-present",
    "install:all": "npm install && npm run build:shared"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
